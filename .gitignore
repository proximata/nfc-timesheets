.firecrawl/

# psst local vault (plaintext secrets)
.psst/envs/

node_modules/

# legacy VM snapshots (timesheets.env holds the OLD plaintext APP_KEY and ADMIN_PIN)
.vm-legacy-backup/
legacy-backup/
.env
.env.local
.env.*.local
.psst/
*.pem
*.key
id_rsa
id_ed25519

# gstack browse scratch — network logs capture live API keys from visited pages
.gstack/

# Xcode per-user UI state - changes on every click, never meaningful in a diff
*.xcuserstate
xcuserdata/

# macOS junk
.DS_Store

# obsolete: legacy demo video (pre decision-20/22) and GitHub Pages AASA (superseded by decision-4)
video/
pages/

# PoC: map-led dashboard (not part of the app or the build)
.poc-map/

# generated verification screenshots: regenerable, and this repo already paid for
# one history rewrite over committed media
docs/media/ia/
docs/media/redesign/
docs/media/redesign-demo/
docs/media/states/
# the LOOK run's phone evidence: 86 MB of full-page PNGs, regenerable with
# `node demo/look-phone.mjs`. Ignored BEFORE the first one was written, because the
# rewrite above was paid for by media that reached a commit first and a .gitignore second.
docs/media/look/
docs/media/look-phone/
docs/media/verdict/
docs/media/break-look/
